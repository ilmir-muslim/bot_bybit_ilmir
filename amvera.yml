meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
run:
  command: python run_bot.py
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
